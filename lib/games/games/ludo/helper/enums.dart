enum PlayerColor {
  red,
  green,
  blue,
  yellow
}

enum TokenType {
  green,
  yellow,
  blue,
  red,
}

enum TokenState {
  initial,
  normal,
  safe,
  safeinpair,
  home,
}

enum GameMode {
  offline,
  online
}

enum GameState {
  waiting,
  playing,
  finished
}
